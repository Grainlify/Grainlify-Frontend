/**
 * Canonical public exports for the dashboard feature.
 *
 * Dashboard routing is owned by `DashboardLayout`, which is mounted directly
 * from `src/app/App.tsx`. The legacy `Dashboard` shell was unused and has been
 * removed to avoid maintaining two competing dashboard entry points.
 */
export { DashboardLayout } from './DashboardLayout'
